language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-website' ]; then cd .. && eval "mv $currentfolder generator-website" && cd generator-website; fi

